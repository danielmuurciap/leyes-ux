---
description: Una lista que viene de datos tiene que tener rama de lista vacía.
tags: [construir, lista]
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit]
---

Crea `src/Pedidos.tsx`: un componente React con Tailwind que muestra la lista de pedidos del usuario a partir de la prop `pedidos`. Solo ese fichero; no instales dependencias ni crees más ficheros.
